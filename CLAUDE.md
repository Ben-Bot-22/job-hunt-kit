# jobs-db

@AGENTS.md

<!--
This file is a stub on purpose. `AGENTS.md` is the source of truth and everything is written there.

`AGENTS.md` is the cross-agent filename (Linux Foundation stewardship, ~60k repos) — a filename
agreement and nothing more: no schema, no sections, just "this is where the instructions are". Most
other agents read it natively. **Claude Code does not**, whatever a handful of secondary sources
claim, so the `@AGENTS.md` line above is load-bearing rather than decorative: delete it and Claude
Code gets a one-word instruction file.

Do not write instructions here. Two copies drift, and the one that drifts is the one nobody is
reading at the time. `core/test_portable_workflows.py` fails if this file grows prose of its own.
-->
