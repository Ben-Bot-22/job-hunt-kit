# Skiplist — jobs Robin Doe has APPLIED to or REJECTED (never surface again)

Robin Doe is fictional and so is every line below; see `config/example/README.md`.

The triage tool reads this file and skips any job whose id matches a line below **before** spending a
fetch or analysis call. Add an id when you apply or reject a role — copy the `id:` the worklist prints
next to each job. One id per line. Text after `#` is a note (ignored by the matcher). Blank lines ok.

Format:  `<id>   # applied | rejected — YYYY-MM-DD, note`

crescent freight|full stack developer dashboard|   # applied — 2026-01-12, remote perm, band $140-160k
tessellate labs|staff platform engineer|   # rejected — 2026-01-13, 8+ years hard-required and Kubernetes ownership; no band posted
pinnacle grid|senior software engineer go|   # rejected — 2026-01-14, production Go required and Robin has none
